import { IconPlay, IconPause } from './components/icons';

import TimeAxis from './components/TimeAxis';

const Page = () => {
  return (
    <div>
      <IconPlay />
      <IconPause />
      <TimeAxis />
    </div>
  );
};

export default Page;
